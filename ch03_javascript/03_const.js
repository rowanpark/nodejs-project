const x = 5;
const y = 6;
const z = x + y;

console.log(z);

// x = 7; // 에러 발생.
// const x = 7; // 에러 발생.

const dateFormat = "yyyy.MM.dd"; // 날짜 포맷은 `연도.월.일`. => `'`로 작성해도 저장하면 `"`로 자동으로 바뀜.
console.log(dateFormat);
