function hello(name) {
  console.log("Hello, " + name + ". Welcome!"); // 더하기 기호를 사용해서 하나의 문자열로 합침
}
hello("Ran");

function hello2(name) {
  let name2 = "Renee";
  console.log(`Hello, ${name} and ${name2}.`); // ${변수명}을 사용하면 문자열 안에서 변수 값을 사용할 수 있게 해줌
}
hello2("Rowan");
